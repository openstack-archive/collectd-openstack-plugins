---
 fixes:
  - |
    Fixes 'bug 1684579 https://bugs.launchpad.net/collectd-ceilometer-plugin/+bug/1684579'.
    Changed the extensions for the back-up .conf files. This prevents that
    back-ups being read if the original is deleted, thus preventing that
    service to run when it is not configured properly.
